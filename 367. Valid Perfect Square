class Solution {//0ms,100%
public:
    bool isPerfectSquare(int num) {
        for(long i=0;i<=num;i++)
        {
            if(i*i==num)
                return true;
            else if(i*i>num)
                break;
        }
        return false;
    }
};
